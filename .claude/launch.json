{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "website",
      "autoPort": true,
      "runtimeExecutable": "python",
      "runtimeArgs": [
        "-c",
        "import os, http.server; port = int(os.environ.get('PORT', '8642')); http.server.ThreadingHTTPServer.allow_reuse_address = True; http.server.ThreadingHTTPServer(('127.0.0.1', port), http.server.SimpleHTTPRequestHandler).serve_forever()"
      ],
      "port": 8642
    }
  ]
}
